import { StyleSheet } from "react-native";

const mainstyles = StyleSheet.create({
    header: {
        marginTop: 50,
        padding:20,
        text:{
            fontSize:30,
        },
    },
})

export default mainstyles;